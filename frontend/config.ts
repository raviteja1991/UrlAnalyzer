const API_BASE_URL = import.meta.env.VITE_REACT_APP_API_URL || 'http://localhost:5000';
export { API_BASE_URL };

    ///The  API_BASE_URL  constant is exported from the  config.ts  file and used in the  App.tsx  file to make the API request. 
